apiVersion: v1
kind: Service
metadata:
    name: "ts0207-0b6b"
    labels:
        app: "ts0207-0b6b"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "ts0207-0b6b"